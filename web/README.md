# web/ — React + Vite + Tailwind frontend

Source for the IPL Intelligence UI. See the root README for the full project.

```bash
cd web
npm install
npm run dev      # dev server with /api proxied to FastAPI on :8000
npm run build    # outputs dist/ (tracked in git; served by FastAPI)
```
